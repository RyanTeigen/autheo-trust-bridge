import React from 'react';
import SmartContracts from '@/components/decentralized/SmartContracts';

const AutheoTab: React.FC = () => {
  // Component remains the same, only the tab label was changed in WalletDashboard.tsx
  return <SmartContracts />;
};

export default AutheoTab;
